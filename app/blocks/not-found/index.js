import React from 'react'

import style from './style'

const NotFound = () => (
    <div className={style['not-found']}>Not found</div>
);

NotFound.displayName = '[block] not-found';

export { NotFound as default };
